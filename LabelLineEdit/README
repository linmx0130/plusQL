plusQL -> pQLabelLineEdit

It is a simple widgets combined a QLabel and a QLineEdit. The two members 
are public so it's easy to use in your program. It shows how to build a 
simple widgets.

public:
	pQLabelLineEdit(const QString & Label_text, 
					const QString & LineEdit_contents,
					QWidget *parent=0);
	QLabel *Label;
	QLineEdit *LineEdit;
